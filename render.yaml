services:
  - type: web
    name: medhub
    runtime: static
    staticPublishPath: .
    buildCommand: echo "static site"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
