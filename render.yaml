services:
  - type: web
    name: medhub
    runtime: static
    buildCommand: mkdir -p dist && cp -R index.html js css img dist/
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
