services:
  - type: web
    name: medhub
    runtime: static
    staticPublishPath: ./public
    buildCommand: echo "static site — всё уже собрано в public/"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
